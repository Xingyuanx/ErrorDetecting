# 第十周周总结（沈永佳）

## 本周完成
- 数据库搭建与初始化：安装并启动 PostgreSQL，创建业务库 `hadoop_fault_db`，执行 `doc/project/数据库建表脚本_postgres.sql` 完成核心表与初始数据导入。
- 后台用户与权限：创建业务用户并授予目标库连接与基础读写权限，记录账户与权限变更。
- 后端框架初始化：在 `backend/.env` 配置数据库连接与 JWT 参数，按模块方式启动后端并完成基础接口联调（健康检查/登录/注册）。
- 文档完善：补充并优化后端启动命令与部署说明，便于跨环境复用。

## 细节与参考
- 数据库连接读取与默认值：`backend/app/config.py:6-21`
- 启动命令（模块方式）：`backend/README.md:73-92`
- 登录接口：`backend/app/routers/auth.py:25-50`
- 注册接口与校验：`backend/app/routers/auth.py:53-93`
- 前端联调入口：`frontend-vue/src/app/stores/auth.ts:33-56, 58-75`；代理配置：`frontend-vue/vite.config.ts:6-18`

## 遇到的问题
- 局域网环境下的远程访问受限：部分主机对数据库连接仍出现超时或认证失败，需调整 `postgresql.conf` 的监听地址与 `pg_hba.conf` 的网段策略，并确认 `5432` 端口开放。

## 下周计划
- 完成局域网访问策略与防火墙端口开放的统一配置，确保后端到数据库的稳定连接。
- 扩展端到端联调用例（登录/注册/当前用户）与失败场景覆盖，完善日志与错误提示。
- 补充 CI/CD 基础流程与脚本，提升交付效率与质量保障。

## 结论
- 本周完成数据库与后端的基础搭建与联调，形成最小可运行闭环；远程访问策略将在下周统一修复，确保局域网环境下稳定联通。
